# remove a single timeline from the statue
$data remove storage crass:statue root.$(uuid).timeline_$(timeline)
$data remove storage crass:statue root.$(uuid).timeline_$(timeline)_length
$data remove storage crass:statue root.$(uuid).timeline_$(timeline)_object

